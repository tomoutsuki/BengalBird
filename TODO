* In main course page, the "island" icon is wrongly used. The icon is actually the image including the circle around the symbol. So it should not generate a circle around the icon, but it just has the icon, and connect the line.
* Instead of just alterning between left and right, make a "path" style, more like a smooth and gentle "S" shape curve. 
* In dictionary random word practice challenge, in easy mode, the nukta is appearing in separated key. It should be a key with nukta already inserted (ড় instead of ড and ় separatedly).
